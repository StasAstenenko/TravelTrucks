export const selectAllTransport = (state) => state.allTransport.items;
export const selectTotal = (state) => state.allTransport.total;
export const selectIsLoading = (state) => state.allTransport.isLoading;
